if (obj === null || obj === undefined || obj === "") {
        obj = "#262729";
      } else {
        const pattern = /^(#([0-9A-Fa-f]{6}))|(hsl\(\d+, ?\d+%?, ?\d+%?\))$/;
        if (!pattern.test(obj)) {
          obj = "#262729";
        }
      }
    if (obj instanceof Color) {
        return obj;
    }
    if (!(this instanceof Color)) {
        return new Color(obj);
    }